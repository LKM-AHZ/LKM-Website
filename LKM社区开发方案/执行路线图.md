# LKM 社区 — 后端演进执行路线图

> 最后与代码同步:2026-09-13(对照 LKM-service commit `9fd2dae`;M2/M3 各阶段状态与落点注记已按实际代码回填)。

> 配套文档:`后端规划.md`(目标蓝图,架构/设计决策权威源,勿在本文件重复论证)。
> 本文件把蓝图第十章「M0–M5」**再拆成可执行的泳道与阶段**,阶段统一四段式:就绪条件 / 动作(含落点)/ 验收(可测)/ 风险与回审。
> 落点措辞指向真实模块/文件/表/事件,供与 `C:\Project\LKM-Website\LKM-service` 现有代码人工核对(本文件不直接改代码)。

---

## 0. 编号、泳道与使用方式

### 0.1 命名规则
- 阶段号沿用父里程碑: `M0.5 / M1.Y / M2.Y / M3.Y / M4 / M5`,每个父里程碑再拆 **`.Y` 有序小阶段**(编制单位是阶段,不再往下拆 Todo)。
- 版本状态行(每阶段卡):`[待启动 → 施工中 → 待验收 → 已验收]`。验收标准以「当 X,测得 Y ≤/= 无 Z」写成可执行断言。

### 0.2 泳道(依赖/先后主视图)

```
稳定基座 M0(已达成)→ 作为全部泳道的就绪前提(基线定义见 §2,不重编排)。
                                                   ▲ 并行
┌──────────────────────── K-BASE 主线(一致性·主力)────────────────────────┐
│   M1 一致性补强          →  M2 模块内聚(整体先行)   →  M3 拆 AUTH 服务      │
│   (outbox/幂等/死信)       (内容聚合根/信息流域)        (独立域+跨AUTH读优化)  │
└────────────────────────────────────────────────────────────────────────┘
   强串行:M1 是 M3 前置,且 M2 在 M3 之前「整体先行」(已确认,非仅拆 AUTH 所需子集先插)。

┌──── M-OBS 指标轨(与 K-BASE 并行,M0 基底就绪即可开)────┐
│   M0.5 指标前移:prom 埋点 / metrics 端点 / 业务指标      │
└─────────────────────────────────────────────────────┘

┌──────── L-GATE 后置按需轨(判据式触发,未达不填)────────┐
│   M4 Pulsar biz 命名空间(判据 §6.5.4)→ M5 APISIX/Infisical/SigNoz/存储   │
└─────────────────────────────────────────────────────┘

┌── M6 补全轨(蓝图缺口补齐,按价值/代价排序;与全部轨道并行,非任何轨道前置) ──┐
│  P0 安全加固/探针 → P1 一致性·GraphQL·批读 → P2 interaction/WS/notification  │
│  P3 触发式(搜索 P1/计数链路/timeline 物化/工程效率) · 不采纳清单见 §9.5       │
└──────────────────────────────────────────────────────────────────────────────┘
   来源:2026-09-15 对《后端规划.md》§一–§十 的逐章实现核对(见 §9 前言)。**非蓝图原定
   里程碑**,故不改 M0–M5 任何阶段卡;P3 沿用 L-GATE 判据式口径,未达判据不实施。
```

### 0.3 依赖与 gate 摘要(先看这张清单)

| gate | 就绪条件 | 后才可推进 |
|---|---|---|
| M1 完成 | outbox+幂等+死信验收绿 | M2(主线;M3 另依赖 M2 gate,见下行)
| M2 完成 | 跨模块 import 归零、内容/信息流聚合根收束 | M3 拆 AUTH(边界先收束)
| M3 完成 | AUTH 独立部署、跨AUTH读 user:snap diff=0、依赖契约成立 | M4(有消费组)/对外里程碑
| M4(可选) | §6.5.4 扇出>2/吞吐/重放 判据成立 | — (无强下位)
| M6 P0/P1 | 无前置(独立补全轨);P2 的 notification 依赖 **M6.7 WS 通用推送** | P3 各项各按自身触发判据 |
| 恒真 | M0.5 可与 M1 并行(埋点不依赖 outbox)**但 outbox 积压指标项例外**(须等 relay 有占位,见 M1.2 前置对应)**

> M2→M3 排固，全部 **M2.Y 阶段**在 **M3.Y 任一阶段**不可并行;M-OBS 与 K-BASE 可并行。

---

## 1. 阶段四段式模板(全文通用)

```
### <里程碑 .Y> — <中英文标题>
| 状态 | [待启动 / 施工中 / 待验收 / 已验收] |
| 依赖 | 就绪前置阶段 或 外部就绪条件 |
| 泳道 | (主线 K-BASE / 指标 M-OBS / 触发 L-GATE) |

**动作(含落点)**:动哪些模块/文件/表/事件/契约的准确指向;收工态定义。

**验收(可测)**:写成「当 X → 断言 Y」(判据/手段/命令)。
- 网关层断言指可 run 的 check(如迁移回滚 assert、curl /metrics)。
- 面向数据的断言给出口径(如抽验 N=user_id 的 diff=0)。

**风险与回审**:违背既定蓝图的、执行中浮现需再度拍板的点子放在此栏,回审记录于 §8。
```

---

## 2. M0 — 基线定义(已达成,不作为本路线图执行项)

> `后端规划.md` M0(生产切 PG / Testcontainers / 错误码+handler / APScheduler 生命周期绑定)已达成,作为以下全部阶段的就绪基座**引用**。
> 本文件不再重排 M0,但把**验收钉在基线上**以防回归:

- **待验证的基线断言(任何阶段改动前 `pytest` 全套应绿)**:集成测试 Testcontainers(PG + 缓存[Dragonfly,Redis 兼容] + 消息总线[目标 Pulsar,过渡 Rabbit])绿;Alembic 迁移在 test-pg 可升可回滚;app 生命周期测试过(调度随 lifespan 正确启停、无悬挂)(这些是 M0.5/M1.Y 的对账准绳)。

---

## 3. M-OBS 指标轨:M0.5 指标前移(可并行)

> 目标:提前掌握 QPS/延迟/错误率,不等 M5。埋点本身不依赖 outbox;唯一例外是**outbox 积压/投递失败**两项计数器需 relay 有上报点(见 M1.2 之后对接)。

### M0.5.1 — 接入 metrics 端点与自动埋点
| 状态 | [已验收] | 依赖 | M0 已达成 | 泳道 | M-OBS |

- **动作(落点)**:`app/api/health.py` 同一路由层挂 `/metrics`;FastAPI 注入 `prometheus-fastapi-instrumentator` 注册;响应含缺省 `http_requests_total / http_request_duration_seconds / http_response_size_bytes / http_request_size_bytes`(默认 export)。
- **验收(可测)**:`curl localhost:8000/metrics` 返回 `text/plain` 且含 `http_requests_total`;打任意 `/api/...` 后该计数 ≥1;抽样校验 histogram `http_request_duration_seconds_bucket` 存在。全 assert 通过则状态「已验收」。
- **风险/回审**:暂无,纯增量(不改原 health 逻辑)。回审记录见 §8(如无则空)。

### M0.5.2 — 业务指标占位(outbox 相关暂挂)
| 状态 | [已验收] | 依赖 | M0.5.1 + relay 上报点 | 泳道 | M-OBS |

- **动作(落点)**:业务侧补发帖量(order/counter,`app/api` 或 `service` 层);队列深度/outbox 积压/投递失败两个指标**先定义指标名与上报点**(如 `outbox_pending_count`、`notify_failed_total`),由 relay(M1.2/M1.3)实现时接线上报;Grafana 建基础面板(至少 QPS/P50/P95/错误率)。
- **验收(可测)**:发布一帖→`post_created_total` +1(exposed 于 /metrics);Grafana 面板可查 QPS/P50/P95/错误率(**四项**)。
- **风险/回审**:若指标轨先于 M1 推进,积压/失败两 gauge 在 relay 未接线前**不报错**——占位上报点返回 0 而不断言(避免假绿、也避免阻塞并行)。标注于 §8。
---

## 4. K-BASE 主线: M1 一致性补强(事务发件箱)

> 全章承接 `后端规划.md` M1;消除「DB 成而 MQ 丢」缺口+leader 选举+消费者幂等/死信收口。本章成果为 M3 的事件失效链路铺底。

### M1.1 — 建 outbox 表 + 单机 relay 串行就位
| 状态 | [已验收] | 依赖 | M0 | 泳道 | K-BASE |

- **动作(落点)**:
  - 建 `outbox_events` 表(**对齐后端规划 §3.1 权威列**:`event_id UUID`(幂等键)、`status(pending|published|failed)`、`topic`、`key`、`payload(JSONB)`、`attempt_count`、`next_retry_at`、`created_at`、`published_at`、防锁死 `locked_at/locked_by`),**以 TimescaleDB hypertable 按 `created_at` 建时间分区/压缩**(§5.1·PG+Timescale 扩展基座:relay 只扫最近热 chunk 防积压扫脏)。新增位置约 `app/models/` + Alembic 迁移;热分区本地索引 `(status, next_retry_at)`。
  - 建 relay 首个实现(poller):按 `(status=pending|failed)`、`attempt_count`、`next_retry_at` 领取未发布窗口 → 投消息总线(过渡 RabbitMQ,目标 Pulsar `persistent://lkm/biz`,§5.0 mapping)→ 置 `status=published`(失败保 pending/failed,指数退避 + attempt_count/next_retry_at);分批扫描防 N+1(单轮命令条数恒定等),§5.1。
  - 业务域写事件走 outbox 抽象 §5.1(经 `core/messaging.py` 逻辑主题↔命名空间映射表,§5.0)。
- **验收(可测)**:迁移脚本在 Testcontainers-pg **可升可回滚**;正常发帖→`outbox_events` 逐行终为 `status=published`,无残留 `status=pending|failed`(无故障时不触发 replay);故障注入(裁总线)→事件停留未发布→恢复后 poller 补齐并发布(置 `status=published`),事件 1:1 无丢无重(按 `event_id` 去重,终态事件集唯一、无重复消费副作用)。
- **风险/回审**:leader 在 M1.1 仅「单 owner」实现——避免单人误当多副本仍可跑的假绿,明确此阶段验收仅覆盖单副本(防 K8s 假象,见 §8)。

### M1.2 — leader 选举(多副本接管)
| 状态 | [已验收] | 依赖 | M1.1 | 泳道 | K-BASE |

- **动作(落点)**:同一时刻仅一个 leader 扫描投递,其余 follower 不轮询;领导权用 DB 租约表+心跳 **或** Redis 分布式锁 TTL 续约 §5.1;`FOR UPDATE SKIP LOCKED` 降为 leader 内多线程兜底;失联即被接管(陈旧锁回收:复查 `locked_at` 超时 → 强取)。
- **验收(可测)**:起 2 副本(pytest + 双进程/`Task` 模拟)→ 仅一个 leader 在 poll(日志断言:其余 follower `[follower] 不轮询`);kill 当前 leader→租约超期被接管,**事件续投无重复(终态事件集唯一)**;在接管窗口内制造并发投递冲突→断言被 SKIP LOCKED 兜底不重复投递。
- **风险/回审**:租约 TTL 与接管延迟权衡(过长→故障恢复慢;过短→无故障频繁抢主)。给基值 TTL,回审若实测出现无故障抢主提高 TTL,见 §8。

### M1.3 — 消费者幂等 + 事件失败收口
| 状态 | [已验收] | 依赖 | M1.1 | 泳道 | K-BASE |

- **动作(落点)**:消费者**按 `event_id`(outbox 全局 UUID,蓝图 §3.1 幂等键定案)去重**,持久化已处理 id(DB 唯一约束 / 去重表,§5.3 §0);业务侧如需"同实体只结算一次"(如计分)再按 `aggregate_key + 业务版本`做次级守约,不作为主幂等键;消费失败进 `event_failures/dead-letter` 收口,持续失败不卡管道(对齐 §5.1:outbox `failed` 计 `attempt_count` 达上限摘除进 `event_failures`);
- **验收(可测)**:重放同一事件两遍→DB 无二次副作用(断言:副作用发生计数器 =1);注入消费者抛错→该事件进 failure 通道、**管道不被卡死**(后续事件仍被消费);命令断言幂等 DB 层唯一约束生效。
- **风险/回审**:—— 暂无越界项(幂等打点可能跨 M2/M3 需回主轨时,复用此幂等框架,不重复造)。

> M1 整体「已验收」判据 = M1.1+M1.2+M1.3 全绿,才能进 M2/M3。若 M-OBS 轨先冲但 outbox 积压指标仍未接线,累积到 M1.3 完成时补:relay 上报前述两个 gauge 实现 M0.5.2 期望项。

---

## 5. K-BASE 主线: M2 模块化收束(整体先行)

> 整章承接 `后端规划.md` M2(模块内聚/聚合根 + §0 演进的收束步骤)+ §7(模块化与依赖方向)。把内容域(content/boards/columns/qa + **Like**)合成**内容聚合根**;follow+timeline 合成**信息流域**;抽 rbac/storage/common 共享基础设施。**在边界已收束前提下,才能进 M3 把 AUTH 拆出去**。全章结束后跨模块 import 归零、强一致不出模块。

阶段拆分:**M2.1** 提取共享 infra 与 import-linter 约束基线;**M2.2** 内容聚合根收束;**M2.3** 信息流域收束 + GraphQL 只读聚合读回归。

### M2.1 — 共享基础设施抽取 + import-linter 约束筑基
| 状态 | [已验收] | 依赖 | M0 | 泳道 | K-BASE |

> **实际执行(2026-09,commit 00a5d71 前后)**:`pyproject.toml [tool.importlinter]` 已落 4 条契约
> ——① `app.core` 不依赖 `app.modules`、② `app.db` 不反向依赖业务模块、③ 业务模块间不任意依赖、
> ④ 业务域不得 import `app.modules.auth`(只经 `auth.snapshot` 读缝);对注册枢纽/worker 消费等
> 既有耦合用 `ignore_imports` 精确豁免。共享层落点 `app/core/`、`app/db/repo.py`、
> `app/modules/registry.py`。门禁命令 `uv run lint-imports`。

- **动作(落点)**:抽出 `core/` 共享(rbac、storage/s3、common util 等)作为确定性层;`import-linter` 规则在仓库强制模块方向(§7.3 依赖方向),后续统一出口。
- **验收(可测)**:`import-linter` 全仓检查通过(违反=0);抽象层不引用具体业务(方向断言)。
- **风险/回审**:—— 无,仅抽取不改行为。

### M2.2 — 内容域收束为单一内容聚合根(Post/Comment/Like + boards/columns/qa)
| 状态 | [已验收] | 依赖 | M2.1 | 泳道 | K-BASE |

> **实际执行(2026-08 底,commit bc13172「内容域四模块合并」/3f0f56c「删除 forum 模块」)**:
> 原独立 `forum/boards/columns/qa` 已并入 `app/modules/content/`(`models.py`/`router.py`/
> `service.py`/`graphql.py` + `boards/`、`columns/`、`qa/` 子包);blog 经 import-linter 白名单
> 引用 content 公共 API;Like 冗余 `like_count` 同事务维护。跨模块 import 由契约三强制。

- **动作(落点)**:把三套文章源(若仍散放)在 `app/modules/content` 收束;清除交叉 import;Post/Comment/Like 同 Repository/ 同 Service 管理强一致(§7.1 边界条);服务上一模块的 repository/service 不被另一模块直接 import;目录合并(过渡期保留 REST 路由不回退)。
- **验收(可测)**:跨模块 import 归零;内容模块内同事务强一致:插入 like → DB 冗余 `like_count` +1(同一事务可见)。
- **风险/回审**:GraphQL 只读聚合 Query(§2 多端点)需求在此同步覆盖,不作为单独阶段重开(见 §8 备注)。

### M2.3 — 信息流域收束(follow+timeline)+ GraphQL 只读聚合读回归
| 状态 | [已验收] | 依赖 | M2.2 | 泳道 | K-BASE |

> **实际执行(2026-08 底,a6f2ed9「timeline 数据源切换」)**:follow+timeline 合并为
> `app/modules/feed/`(`feed.py`/`models.py`/`router.py`/`service.py`/`graphql.py`),读模型由
> 多源实时 join 收敛为 feed 聚合;只读 GraphQL Query 随各域模块保留(`content/graphql.py`、
> `columns/graphql.py`、`feed/graphql.py`、`blog/graphql.py`),REST `/api/v1` 契约未回退。

- **动作(落点)**:合并 `follow+timeline` 为信息流域模块(§7.1);清 cross-import;`rbac/storage/common` 全部进共享;GraphQL 只读 Query 与 REST `/api/v1` 在过渡期都可跑(目录合并+路由拆片兜底,§2);
- **验收(可测)**:跨模块 import 归零(同 M2.2 断言);读/写路径回归:follow→timeline 可查(集成测试);GraphQL 只读 Query 仍可用(通过旧 version 不破)。REST 返回不变(契约测试)。
- **风险/回审**:读侧聚合若 timeline 依赖的多个源现仍分空间同步 Query(group:见 §6.5.2)在 M2.3 阶段先给读模型/游标落地机会,若决策在细化时反转请见 §8。

> M2 完成判据=M2.1..M2.3 全绿 + 跨模块 import 归零 + M0 回归全绿。**此时才能开始 M3**。

---

## 6. K-BASE 主线: M3 拆 AUTH 独立服务 + 跨AUTH读模型精简

> 承接 `后端规划.md` M3 + §4/§5.4/§2(启动就绪三探针)+ 记忆已定向(单向权威边界、user_dim 报表用、outbox 事件失效)。
>
> **实际执行(2026-09-05~09-06,commit M3 / M3B / M3B1 / M3C / M3D / M3E)**:auth 已**物理拆库 + 独立
> 进程**——`users/profiles` 迁出 monolith `Base.metadata`,改由 `app/db/auth_base.py`(auth 独立库
> metadata)承载,业务表行只引用裸 int `user_id`(`deploy/initdb/01-auth-db.sh` 建 auth 库);独立 ASGI
> 进程 `app/main_auth.py`(compose 服务 `auth`,`python -m app.main_auth`),业务读身份一律经
> `app/modules/auth/snapshot.py` + `user_http.py` seam,由 import-linter 契约四强制。以下 M3.0–M3.4
> 状态据此判定。

阶段拆 **M3.0** 拆出强内聚 auth(边界经 M2 已收束) → **M3.1** 独立部署与接口 + 进程内 HTTP 自持 → **M3.2** `user:snap` 缓存 + AUTH 事件失效 → **M3.3** user_dim 报表 ETL(运营/后台隔离) → **M3.4** 启动就绪三探针 + 硬/软依赖契约。

### M3.0 — auth 收敛为强内聚模块(独立认证库)
| 状态 | [已验收] | 依赖 | M2 全绿 | 泳道 | K-BASE |

> **实际执行**:auth 表集仅存于 `app/db/auth_base.py`(独立库 metadata);`app/modules/auth/`
> 不含对业务库/业务模块的 import(契约四),可独立组装 DB 存活。

- **动作(落点)**:`app/modules/auth/` 先收一圈无内部遗留 cross-import;JWT/密码/2FA 归 `CORE:`受控侧 §4;确认 `app/modules/auth` 里不出现对主业务库的直接耦合(仅引用自身域)。
- **验收(可测)**:import-linter 断言 `auth -> 主业务` 方向=0;`auth` 单独组装 DB 存活(用于将来独立)。
- **风险/回审**:auth 与主业务共享的公共表引用?若无(现实为独立库)直接可拆;如有共享残留提前在 §8 提出隔离取舍。

### M3.1 — 编译为独立服务进程 + 接口暴露 + 进程内 HTTP 自持
| 状态 | [已验收] | 依赖 | M3.0 | 泳道 | K-BASE |

> **实际执行**:compose `auth` 服务跑 `app.main_auth:app`;主服务经 `auth_http_url`/`auth_http_token`
> seam(`app/modules/auth/user_http.py`)调 AUTH,主业务 graph 无 `users` 表 join。

- **动作(落点)**:把 auth 从单体镜像拆到独立可部署(deploy 自理、队列 `/auth/**`、`/users/me` 由 APISIX 转到 AUTH §0);主服务进程内 HTTPClient 调 AUTH 只读 API(client 装配于 `clients/UserAPIClient`)。
- **验收(可测)**:认证接口独立部署跑通(独立 image up → `/auth/login`+`/users/me` 走 AUTH 200);主服务内无 `user` 表直接 join(静态断言:无 `from ... user` 在主业务 graph)。
- **风险/回审**:主服务对 AUTH 启动不阻塞——于此阶段即验证懒加载/失败降级不 crash(否则 m3.4 是后验,太晚)。当失败无法唤醒时见 §8。

### M3.2 — 跨 AUTH 在线读 user:snap 缓存 + 变更即失效(AUTH 发事件消费失效)
| 状态 | [已验收] | 依赖 | M3.1 + M1 事件链路 | 泳道 | K-BASE |

> **实际执行**:`app/core/user_cache.py`(cache-through + 版本 CAS + epoch 防复活);`app/modules/auth/events.py`
> 发 `user.updated / banned / session_revoke` outbox 事件,worker `app/modules/auth/tasks.py`
> (`invalidate_user_snap`)消费后仅失效不写值;测 `tests/test_user_change_invalidation.py`。

- **动作(落点)**:§5.4 on-read:进程 HTTPClient 回退 API 回填 `user:snap` 缓存(§5.6 带来源版本 CAS/版本守卫,只准新版本写赢);AUTH 发 `user.updated / banned / session_revoke` 事件→主服务消费**仅失效** `user:snap`(不写缓存值,读靠 API 回退)。
- **验收(可测)**:
  - 抽验 Ni 个缓存命中 user_id:`user:snap` 与 AUTH API 返回 **diff=0**(口径 §5.6);
  - 并发回填 + 失效:断言 `user:snap` 旧版本**不能顶回失效**(LWW 守卫生效)——构造并发,老版本携带旧版本号不可 write;更新后读必拉到新值(无陈旧)。
- **风险/回审**:**跨缓存失效只失效 user:snap**,不失效 user_dim(报表解耦)——否则双失效一致性复杂度卷土(见记忆 §5.4 简化双读模型)。任何要在失效时也清 user_dim 的冲动请 §8 回审。另 AUTH 事件在统一总线走 `persistent://lkm/auth`(过渡仍 RabbitMQ 用户域/`lkm.events`),避免业务域跨命名空间失效(仅在 M4(§7.1)据判据真正把业务 topic 迁入 `persistent://lkm/biz` 后视需同步失效)。

### M3.3 — user_dim 报表宽表 ETL(离线/批量,运营/后台专用)
| 状态 | [已验收] | 依赖 | M3.2 | 泳道 | K-BASE |

> **实际执行**:宽表 `app/db/user_dim.py`;ETL `app/modules/auth/user_dim_sync.py`(批量常数命令,
> 无 per-row N+1);离线只读 port `app/modules/admin/dim_report.py`;测
> `tests/test_admin_user_dim_report.py`。在线读写仍走 `user:snap`/auth 缝,未读 dim。

- **动作(落点)**:`etl/` 周期增量同步(updated_at > last_sync 增量为主,每日/低全量兜底)灌 `user_dim` 宽表(单向、只读副本 §3.1);任何修正回 AUTH(无写回 AUTH 以外路径)。
- **验收(可测)**:报表查询只走 `user_dim`;发一修账号字段到 AUTH → 下一拉批后 `user_dim` 同行更新;后台列表经 `user_dim` **与在线读(user:snap)隔离**未被后者的失效波及。增量同步的性能:batch 不逐条(断言单 poll 命令数恒定)。**此表服务运营/后台 `报表列表`,仅供离线**;在线读绝不读 user_dim。
- **风险/回审**:**别让报表宽表变成在线读源**(本表用途窄;在线读一致由 M3.2 user:snap)。若项目方坚持 per-request 从宽表聚合实时 → 违反既定;§8 标注.

### M3.4 — 依赖契约:启动不阻塞 + readiness 复合 + liveness 自足
| 状态 | [已验收] | 依赖 | M3.1 | 泳道 | K-BASE |

> **实际执行**:AUTH 进程专属 `app/health_auth.py`(liveness 零外部依赖 / readiness=DB+Redis);
> 单体 `app/modules/health/router.py` 复合探测 DB+Redis+AUTH,且自身 liveness 面不受 AUTH 不可达
> 拖累;测 `tests/test_auth_process_health.py`。

- **动作(落点)**:启动时**不阻塞 AUTH**(懒加载、验签 JWKS 公钥缓存加载§2;缺失时「验证暂不可用」降级但 service 存活);readiness 定义为 DB/L2 缓存(Dragonfly,Redis 兼容)/**Pulsar 消息总线(过渡期指现 Rabbit)**/**AUTH**(四项复合硬依赖,AND);liveness **不依赖**任何外部(自足心跳);软依赖(Pulsar `lkm/biz` 业务域不作为门槛;搜索/MinIO)缺失不降 readiness;部署三探针(startup/readiness/liveness)按此接 §6.4/§2。
- **验收(可测)**(以下做成可自动的故障注入):
  - 停 DB/L2 缓存(Dragonfly)/Pulsar 总线(过渡 Rabbit)/AUTH 任一硬依赖 → 主服务**进程不重启**(liveness 200)→ `readiness` 复合**未就绪**(503);
  - AUTH 未先于主服务起来(实际 依赖契约 AUTH 可后起)→ 主服务不阻塞(可 start);
  - 软依赖缺失(停搜索/MinIO)→ readiness 仍通过;
  - JWKS 公钥未就绪 → 接口返回受控(如 401/503「验证暂不可用」)而非崩。
- **风险/回审**:**AUTH 独立导致主服务对它懒加载的调用可能成瓶颈**(用户补 M3.0 收成的 `UserAPIClient` 打满 AUTH 时 - singleflight+读回退是既定兜底)。当 QPS 提升实测打满 AUTH 时,若是回退缓存命中低,按既定升 user:snap 命中而非重读 user_dim(见 §8)。

> M3 完成判据 = M3.0..M3.4 全绿 + M2/M1 回归全绿。进入对外可用里程碑(文档评审点可在 M3.4 落一节给回审「能力稳定」版块)。

---

## 7. L-GATE 后置触发轨:(触发判据驱动,未达不填,达阈值回主轨)

> `后端规划.md` §0 演进第 2 条明确:存量 RabbitMQ 单总线复用为**过渡地基/（目标统一到 Pulsar `lkm/auth`）**;目标按判据(§6.5.4)把业务 topic 渐进落到 Pulsar **`lkm/biz`** —— 先从最需扇出的 notify 拆起,增量映射表幂等。**本文件不给固定序号,给「触发→执行→验收」闸门。**

### 7.1 — M4 触发闸门:Pulsar 业务命名空间(`lkm/biz`)topic 当 §6.5.4 判据成立才拆
| 状态 | [已实施提前,回审见 §8 #10] | 触发条件 | §6.5.4 判据任一命中:扇出单事件 2+ 异需消费者 / 峰值超单机舒适区 / 需统一 lag/重放 / 某消费者故障想隔离于其余 |

> **实际执行(2026-09-13)**:判据未达但经用户拍板**提前全量迁移**——不止 biz,连 auth/cron
> 全量落 Pulsar,RabbitMQ 物理下线(`aio_pika`/`amqp.py`/compose `rabbitmq` 移除)。新增
> `core/messaging.py` 逻辑 routing_key→topic 映射与 JSON schema;落点:auth/email、auth/user.events、
> biz/notify.upload、biz/points.apply、system/cron、system/dlq。**points 拆 reward/stats/tasks
> 三个 Shared subscription**(同 topic 扇出,独立进程→真实故障隔离),满足「≥3 下游消费无串扰」
> 验收;死信经 Pulsar DeadLetterPolicy 落 `system/dlq` 后由 worker-dlq 落库复用 admin requeue;
> lag 经 Admin REST 上报 `pulsar_subscription_backlog` gauge。详见 §8 #10 回审登记。

- **动作(落点)**:核心于**从现有 `lkm.notify`(Rabbit,过渡)拆出**,落 Pulsar **`persistent://lkm/biz/notify.*`**(+ 其它按判据扩张的 biz topic);经 §5.0 `core/messaging.py` 逻辑主题→命名空间/topic 映射表把这些主题整批 commit(业务 publish/subscribe 无感);计数/索引/feed 各自用 **Pulsar subscription** 消费;Schema Registry 校事件(用 Pulsar 自带/Pulsar schema registry);死信/重试落 **Pulsar 死信 topic+TTL/重投策略**(过渡 rabbit 亦然);此后 Rabbit 收敛为过渡用户段(仅 email/audit/`user.*`),直至迁移完成。
- **验收(可测)**:新启 ≥3 下游 topic order消费无串扰;lag 可观测;故障隔离:停一个消费组不妨碍其它(断言其余仍消费)。无判据时不强制实施(此节不做也可以发到验收)。
- **风险/回审**:**迁移不引起伪双命名空间**(未欠系统判据) ← 本文不做硬更新但有判据时执行。业务 topic 进 **`lkm/biz`** 前,rabbit 仅作过渡用户域(目标 `lkm/auth`);避免未到量就为表象搬移、避免重复建模,§8。

### 7.2 — M5 闸门:容量与治理(规模化后置增强:APISIX|Infisical|SigNoz|ClickHouse|双级缓存|msgspec|hypothesis 等)
| 状态 | [待验收] | 触发条件 | 达到规模化阈值 / 已具业务体量 |

> **执行现状(2026-09-13)**:Pulsar 迁移已提前完成(见 §7.1 / §8 #10)并做了 worker/task_registry
> 收口(commit `M5.1`)。
>
> **7.2.x 双级缓存 L1 首级(2026-09-13 已实施)**:`user:snap` 热读加本地进程内 L1
> (`app/core/local_cache.py`,有界 TTL+LRU、仅加速不具权威)+ singleflight 请求合并
> (`app/core/singleflight.py`,引用计数回收防无界锁泄漏);跨实例失效经 Redis pub/sub
> (`app/core/user_cache_events.py`,订阅方只删 L1 不 DEL L2);配置 `LKM_USER_SNAP_L1_ENABLED/_TTL_S/_MAXSIZE`
> 与 `LKM_USER_SNAP_SINGLEFLIGHT_ENABLED`;指标 `user_snap_cache_total`/`user_snap_singleflight_total`。
> **perf 基线已补**(见 7.2.x 基准):`loadtest/bench_read_serialize.py` 实测 L1 较 L2 快 ~18.8x
> (fakeredis,4.1µs vs 76.9µs/read),满足「相对启用前非恶化」。偏离与边界见 §8 #12。
>
> **7.2.x 读热 msgspec 序列化(2026-09-13 已实施)**:timeline 读热端点在 Pydantic 校验后改走
> msgspec(`app/core/wire.py` 通用 Response/envelope + `app/modules/feed/wire.py` Struct 镜像),
> `err._wrap_result` 支持预编码 Response 透传;配置 `LKM_READ_MSGPEC_ENABLED`(关闭回退旧路径)、
> 基准 `loadtest/bench_read_serialize.py` 实测 20/50/100 条响应 **6.3–7.1x**、省 90–334µs/req 且
> JSON 等价 → 默认开启。字段名保持 snake_case(未按蓝图 `to_camel`,见 §8 #13)。首期仅 timeline,
> 其它列表端点待收益复评再扩。
>
> **7.2.1 属性测试 hypothesis(2026-09-13 已实施)**：`tests/prop_pg.py`（持久 loop + 独占 PG
> schema 的同步 `@given` 运行器，避 hypothesis×asyncpg 跨 loop）+ `tests/test_outbox_props.py`
> （退避单调有界；enqueue 显式 `event_id` 幂等；relay 随机失败序列下不重复投递、达上限折叠
> `EventFailure`）+ `tests/test_user_dim_props.py`（sync 恒 2 命令、reconcile 恒 4/2 命令、二跑
> 收敛 diff=0）；profile 注册于 `tests/conftest.py`（deadline=None、max_examples 收紧、抑制
> `function_scoped_fixture` 等，避与 `filterwarnings=["error"]` 冲突）。验收
> `uv run pytest tests/test_outbox_props.py tests/test_user_dim_props.py`（4 项绿）；
> `--hypothesis-seed` 可复现。回退：删两测试文件 + dev 依赖。
>
> **7.2.2 OTel 埋点 + Collector(2026-09-13 已实施)**：`app/core/tracing.py`（fail-open；**显式传
> tracer_provider、不设全局**——全局只能设一次且测试反复 setup 会被忽略；FastAPI/SQLAlchemy/httpx
> instrument，`main.py`/`main_auth.py` lifespan 接线，auth 进程 `service.name` 后缀 `-auth`；
> Pulsar publish/consume 手工 span，traceparent 经消息 properties 注入/续链）；`app/core/logging.py`
> JsonFormatter 增 `trace_id/span_id`（读当前 span，惰性 import，无埋点时静默缺席）。配置
> `LKM_OTEL_ENABLED`（默认 false）`/_EXPORTER_OTLP_ENDPOINT/_SAMPLE_RATIO/_SERVICE_NAME/_HEADERS`；
> 新增 `deploy/otel/otel-collector.yaml` + compose `otel-collector`（`profiles:["otel"]`，不启不影响
> 主栈，内部指标 `:8888/metrics` 可查接收 span 数）。验收 `tests/test_tracing.py`（默认关 no-op；
> 不可达 endpoint 下 setup 不抛、shutdown <3s；InMemory exporter 断言 `GET /` 产 span 且日志
> trace_id 与之一致）。回退：`LKM_OTEL_ENABLED=false`。
> **真机验证(2026-09-14)**：`--profile otel` 起 collector，`LKM_OTEL_ENABLED=true` +
> `LKM_OTEL_EXPORTER_OTLP_ENDPOINT=http://otel-collector:4318/v1/traces` 重建 backend/auth，
> 打 health/graphql 后 collector `otelcol_receiver_accepted_spans=18 / refused_spans=0`，
> debug exporter `sent_spans=18`（OTLP/HTTP 通路与采样生效）。
>
> **7.2.3 Infisical 自托管 + 启动拉取 + SecretStr(2026-09-13 已实施)**：compose 增
> `infisical`/`infisical-db`（`profiles:["infisical"]`，独立 PG 库）；应用统一入口
> `LKM-service/deploy/docker-entrypoint.sh`（Dockerfile `ENTRYPOINT`）在 exec 原 command 前跑
> `python -m app.core.secrets_bootstrap` —— 经 universal-auth 登录拉 `LKM_*` raw secrets 写
> `os.environ`（**已存在的环境变量优先**，Infisical 只补缺；`LKM_INFISICAL_ENABLED` 默认 false
> 不触网；失败时 `REQUIRED=true` fail-fast 否则回落 .env）。**选 entrypoint 包装而非 Settings
> 自定义 source**：`config.py` 导入期即 `Settings()`，source 会在 tests/alembic/11 进程每次 import
> 触发网络。同时把 12 个密钥字段改 `SecretStr`，读取统一经 `app/core/secrets.py:reveal`
> （兼容测试直赋 `str`）；生产 validator 仅补「条件字段」校验（`storage_backend=s3` 的 S3 键、
> 配了 `auth_http_url` 的 seam token），**不按进程强校验 db/redis**（worker 进程 env 集不同，会误杀）。
> 验收 `tests/test_secrets_bootstrap.py`（注入范围/已有优先/失败降级与 fail-fast）+
> `tests/test_config_secrets.py`（条件校验/dev 放行）。回退：关 `LKM_INFISICAL_ENABLED` + 回 .env。
>
> **7.2.4 APISIX 全量替换 nginx(2026-09-13 已实施,待运行时验收)**：新增 `deploy/apisix/`
> ——`config.yaml`(standalone、`config_provider: yaml`、无 etcd、`dns_resolver 127.0.0.11`、
> 9080/9443) + `apisix.yaml`(路由模板:双域名、精确 `/api/v1/admin/auth/me` 优先、auth 前缀、
> `/api`、GraphQL `enable_websocket`、`/_astro/`+`/static/avatars/` 长缓存、`/lkm/*` 经
> `proxy-rewrite.host=lkm-ahz.ltd` 保 S3 预签名、.icu→static、http→https 301、ACME challenge;
> 插件 `client-control.max_body_size=100m`(上传路由)、global `gzip`、`cors`、登录面 `limit-count`
> policy=local; upstream 用 DNS `discovery_type: dns`) + `render.sh`(standalone ssl 只收**内联 PEM**,
> 由 sidecar 读 certbot 证书渲染 `# __SSL_SECTION__` 占位、缺证书生成自签占位、每 6h 重渲染触发
> reload;单文件就地写保 inode) + `acme-webroot.conf`/`smoke.sh`。compose:`apisix`(80:9080/443:9443)
> + `apisix-render` + `acme-webroot`(微型 responder,非网关角色)。
> **注(2026-09-16)**:原「旧 `nginx` 移入 `profiles:["nginx-gateway"]` 作快速回退」的路径**已删除**,
> 理由与替代回退机制见 §8 #25。
> 验收:`tests/deploy/test_apisix_config.py`(路由/优先级/插件/TLS 渲染/转发头 + nginx 移除防回潮) + `smoke.sh`
> (301/health/GraphQL/官网/登录 429/MinIO/缓存头/WS upgrade/上传边界)。
> **运行时验收(2026-09-14,全栈真机 up 后 `SMOKE_HEAVY=1 sh deploy/apisix/smoke.sh` 13/13 绿)**,
> 发现并修复 7 处静态测试漏网的真实缺陷(详见 §8 #19):
> ① `config.yaml` `dns_resolver` 须数组(裸字符串启动即校验失败,旧静态断言同错故假绿);
> ② standalone 形态须 `role: data_plane`+`role_data_plane.config_provider: yaml`(非 traditional);
> ③ 3.9 起 http2 只能声明在 `apisix.enable_http2`(listen 级报废弃致命错);
> ④ `discovery.dns.servers` 须显式初始化,否则 `discovery_type: dns` 全部 503;
> ⑤ http→https 重定向 Location 带内部端口 `:9443` → 加 `plugin_attr.redirect.https_port: 443`
>   (容器非 root 不能直听 443);⑥ MinIO 路由 `pass_host: rewrite` 的 Host 须落
>   `upstream.upstream_host`,用 `proxy-rewrite.host` 被 nil 覆盖 → 空 Host → MinIO 400;
> ⑦ `/api/v1/ws/*` 未开 `enable_websocket`,`Upgrade` 头被剥 → 后端收普通 GET 404(旧 nginx
>   同缺,属既有隐患,一并修复);另 `smoke.sh` 须 `--resolve` 带 SNI + `--noproxy '*'` 绕宿主代理。
> compose 侧修复:4 个 worker(notify/scheduler/dlq/outbox)漏下发三主密钥致生产校验崩溃;
> `pulsar-init` 误用 `--url`(应为 `--admin-url`)致退出 1;APISIX 浮动 tag 拉取失败固定 `3.9.0-debian`;
> APISIX healthcheck 用 curl 但镜像内无 curl → 长期 unhealthy(改 bash `/dev/tcp` 端口探活)。
> **已补验(2026-09-14)**:①证书换发后 render→APISIX 自动 reload(换新自签证书后 2s 内生效,
> render.sh 同 inode 写生效);②DNS discovery 后端换 IP 自愈(用占位容器抢旧 IP 强制 backend 换
> 新 IP,别名保留下 ~5s 恢复健康)。注:用 `docker network disconnect/connect` 手工搬容器会**丢
> compose 服务别名**导致 DNS NXDOMAIN,是测试手法假象,非 APISIX 缺陷——改 IP 测试须用占位容器
> 抢 IP 后 `docker compose start` 保别名。
> **仍待人工**:X-Real-IP 等转发头落上游的实值(需 header echo 上游;已由 global rule 设;
> 后端/前端当前均不消费该头,风险低)。
> 回退:**git**(`git revert` / 检出历史 tag → 整套配置一致);原 nginx profile 回退路径已于
> 2026-09-16 删除,见 §8 #25。
>
> **7.2.5 Prefect 数据管道(2026-09-14 已实施,待真机复核)**:按蓝图 §调度「APScheduler 只做
> 简单 cron 触发入口、复杂管道由 Prefect flow 编排」落地。首个管道 **user_dim**:
> `app/flows/user_dim.py`(flow + 注入式纯编排 + ids 回填 CLI)、`app/flows/deploy.py`(注册
> deployment);`app/modules/auth/tasks.py` 的 cron handler 在 `LKM_PREFECT_ENABLED=true` 时经
> `run_deployment(timeout=0)` 触发、失败 **fail-open 回落直调**(crash-safety 对账不漏跑);
> compose 增 `profiles:['prefect']` 的 `prefect-postgres/server/init/worker`(独立 prefect 库,
> UI 不发布公网端口);依赖 `prefect>=3.8.5,<4`(支持 py3.13);配置 `LKM_PREFECT_*`(默认关,
> 条件化生产校验)。flow 复用既有 ETL 入口,守「命令数恒定 2/4、跨 auth+业务双会话、幂等」。
> 验收:`tests/test_flows_user_dim.py`(编排/命令数/回填幂等)、`tests/test_prefect_trigger.py`
> (开关关直调/开触发/失败回落);真机 `--profile prefect up -d` → deployment run 完成。
> 回退:置 `LKM_PREFECT_ENABLED=false`(默认)回直调,`--profile prefect down` 不影响主栈。
>
> **7.2.6 ClickHouse 分析管道(2026-09-15 已实施,真机已复核)**:按蓝图 §6.2 落地两路——
> ① 周期导出:Prefect flow `app/flows/analytics.py`(纯体层 `app/flows/analytics_body.py`
> **不 import prefect**,故回落直调零 Prefect 依赖)把 `event_failures`(业务库)+`audit_logs`
> (auth 独立库)经 **CH 侧 `max(id)` 水位**增量批量导出,入口分置 owner 侧
> `app/db/event_failure_export.py`/`app/modules/auth/audit_export.py`;cron
> `cron.analytics_export`(每日 03:30)经 jobs 订阅触发,`LKM_PREFECT_ENABLED=true` 且配
> `LKM_PREFECT_ANALYTICS_DEPLOYMENT` 时走 Prefect、失败 **fail-open 回落直调**;`app/flows/deploy.py`
> 改列表驱动注册两个 deployment。② 日志采集:vector 采容器 stdout/stderr 的 `core/logging.py`
> JSON → `lkm.app_logs`(`deploy/clickhouse/{init.sql,vector.toml}`;非 JSON 行保留原文置
> `level=unknown`)。查询面:admin 只读端点 `GET /api/v1/admin/analytics/{dataset}`
> (表/列/排序**白名单常量** + 参数化 + `admin.analytics_view` 权限点;CH 未启用/不可达返
> **503 不返空列表**)。形态:compose `profiles:['clickhouse']` 的 `clickhouse`
> (24.8-alpine,数据卷首启跑 init.sql)+`vector`(挂 docker sock/日志目录);CH 客户端基座
> `app/core/clickhouse.py`(seam + 懒建 + fail-open);表 TTL 30/180/365 天(固定值,非配置化)。
> 验收:`tests/test_clickhouse_client.py`、`test_analytics_export.py`(批量恒命令数/水位推进/
> 重跑 diff=0/失败上抛)、`test_flows_analytics.py`、`test_analytics_trigger.py`、
> `test_admin_analytics_router.py`(白名单/limit 裁剪/503)、`tests/deploy/test_clickhouse_config.py`;
> 真机 `--profile clickhouse up -d` → `clickhouse-client --query "SELECT count() FROM lkm.app_logs"`。
> 回退:置 `LKM_CLICKHOUSE_ENABLED=false`(默认)即 no-op,`--profile clickhouse down` 不影响主栈。
> **真机验收(2026-09-15,`--profile clickhouse up -d` + 挂载新代码的 worker run)**:CH 三表建成、
> vector 采集入库(7.4 万行,pulsar 非 JSON 走兜底/`lkm.*` JSON 分支 level·logger·msg·ts 解析
> 正确)、真实 `clickhouse-connect` async 客户端连通、导出 flow 两 task + flow 全 Completed、
> CLI 退出无连接泄漏。**真机抓出 3 处静态测试/单测漏网的缺陷(均已修并回填断言)**:
> ① `init.sql` 用 `DateTime64` 列直接作 TTL 表达式 → CH 报 `BAD_TTL_EXPRESSION` 并**中止整个
> init.sql**(三表全未建);改 `TTL toDateTime(col) + INTERVAL n DAY`。
> ② vector 镜像自带 `/etc/vector/vector.yaml`(demo 配置),而 vector 默认 config 查找**yaml 先于
> toml** → 未显式 `--config` 时静默采集 demo 假日志;compose 补
> `command: ['--config','/etc/vector/vector.toml']`。
> ③ `clickhouse-connect` 未加 `[async]` extra → `get_async_client` 运行期 `ImportError`(缺
> aiohttp);依赖改 `clickhouse-connect[async]>=0.8`。前两项已写成 `tests/deploy/test_clickhouse_config.py`
> 断言防回归。另:CLI 一次性进程补 `clickhouse.close()`(避免 Unclosed connector)。
>
> 其余容量/治理项:ClickHouse 之外,**可选 TimescaleDB 连续聚合**与搜索/读聚合仍按需未启动(见下动作项)。
> 本节六项(hypothesis/OTel/Infisical/APISIX/Prefect/ClickHouse)均已落地代码与静态验收，状态置 **[待验收]**
> 待真机连跑与上表人工项复核。

- **动作(落点)**(对齐 `后端规划.md` M5 范围,可在细分 7.2.1–7.2.n 滚进;本闸门不开而进入此前阶段卡不发生产配置升级):
  - 网关/密钥/可观测:nginx→**APISIX**(限流/熔断);`.env`→**Infisical**(无明文,启动 unwrap→注入 Settings);OTel→**SigNoz**(metrics/trace/log 一体化);日志/行为/`event_failures` 周期灌 **ClickHouse** 分析。
  - **双级缓存启用**(§5.6;此前阶段按 L2 单级交付则可):L1 = 本地进程内(aiocache 首级)+ L2 = 分布式 **Dragonfly**;对账/防 LWW 收敛方向不变(L2 为一致锚点,L1 仅读加速)。
  - **读热路径序列化 msgspec**(§6.5.2/§5):feed/timeline 聚合读列表在 Pydantic v2 校验后,出端口以 msgspec(`to_camel`)+ 预组装 projection 序列化;基准测试确认真实收益后再扩读模型面,不撤校验(维持「Pydantic 校验 + msgspec 序列化」分工,§1)。
  - **属性/假设法测试 hypothesis**(§6.4):对 outbox 幂等/并发边界、对账收敛防死循环自动生成用例,不破坏 pytest 结构与既测。
  - 复杂数据管道→**Prefect flow**(DAG/失败重试/回填),APScheduler 仅做 cron 触发入口。
  - 可选:**TimescaleDB 连续聚合**服务度量/行为报表(§6.2);搜索/读聚合按需启用(§6.5.2/§6.5.3)。
- **验收(可测)**:部署无明文密钥(endpoint/env 断言);S3 切 OSS/COS 后端不重启业务读写正确(黑盒);单请求全链路 span 完整;读热基准对比现状序列化确认 msgspec 收益才扩(否则维持 Pydantic 序列化);`hypothesis` 套件绿且覆盖明显边界(断言语料自动生成可复现崩溃输入);启用 L1+L2 后读热命中率/延迟相对启用前非恶化(与启用前基线比较);达到目标 QPS。全套不强制一次性,拆分滚进 7.2.1–7.2.n 亦可。
- **风险/回审**:统一存储抽象保持单一实现,勿为供应商多 factory;OSS virtual-host 寻址差异按 §6.3 吸收。Infisical 时机此轨较后,若管理要求提前可移 M3 段(登记 §8)。L1+L2、msgspec、hypothesis 为 M5 后置增强,M1–M4 不以其为前置阻碍(避免把默认快路径押在可后置优化上)。

---

## 8. 回审/越界登记表(本文件执行中出现需再次拍板的点 **只能在此登记 → 请示再改架构**,不边做边擅改)

| # | 阶段 | 主题 | 现状既定(蓝图/记忆) | 反例/待议 | 回审动作 |
|---|---|---|---|---|---|
| 1 | M1.1 | 单 owner 直连假绿 | outbox relay 单副本也可跑 | 易把单进程直连当多副本真绿 | M1.2 前不要宣称「高可用」;验收声明限单副本 |
| 2 | M2.3 | GraphQL 只读聚合 Query 回归窗口 | 多端点版本化 §2,只读 Query 随模块收束同步覆盖 | 细化易把只读 Query 拆成独立阶段 | 并入 M2.3 回归验收,不另起阶段 |
| 3 | §6.5.2(承接 M2.3 决策反转时的回审口) | timeline 读模型(物化 vs 实时 join) | 物化+游标优先,实时 join 是兜底 | 细化工程一反复现风险 | M2.3 首次落读模型,后续欲改先回审 |
| 4 | M3.3 | 报表宽表变在线读源 | user_dim 只报表/离线;在线读走 user:snap(即§5.4) | 有人让 per-request 从宽表聚合 | 回审,禁止绕过 user:snap |
| 5 | M3.0 | auth 与主业务共享残留 | auth 独立库,不共享表 | 若有共享残留需隔离取舍 | 提前 §8 申请判据隔离 |
| 6 | M4 | Pulsar 伪命名空间(biz) | 未达判据只用过渡 Rabbit/用户域(`lkm/auth`);达判据落 `persistent://lkm/biz` | 为表象提前迁移到 Pulsar biz 造复杂 | 判据闸门 M4 见 §7.1,未达不实施 |
| 7 | M5 | 供应商抽象堆叠抽象 | 单 S3 兼容抽象可切 4 供应商,唯配置层吸收 | 为供应商多 factory 过度设计 | 不造 factory,M5 见 §6.3,配置层吸收 |
| 8 | M-OBS/M0.5.2 | outbox 指标与 relay 时机 | 指标口先行但不接线不报错 | 假绿出 | M1.3 补接线时后对账 |
| 9 | M5(7.2) | 后置增强被当成 K-BASE 前置门槛 | L1+L2、msgspec、hypothesis 属 **M5 容量/治理后置增强**,蓝图要求 M1–M4 不以其为前置 | 执行中把 msgspec/hypothesis/双级缓存抬成早闸门、或把它们的默认快路径过早替换 | 维持既有默认(Pydantic 序列化、单级 L2、不押属性测试)直至 M5 闸门(7.2)开启后在 7.2.x 内部滚进 |
| 10 | M4(7.1) | **提前全量迁移 Pulsar,Rabbit 物理下线,偏离触发闸门** | §7.1 为判据式闸门(扇出/吞吐/lag/隔离任一成立才拆);蓝图要求 Rabbit 收敛为**过渡用户段**(仅 email/audit/user.*),biz topic 按判据渐进落 `lkm/biz` | 判据 §6.5.4 未达即全量迁移,且把 auth/cron 一并迁、删除 Rabbit 回退路径;points 拆三订阅打破原单事务强一致(改次级幂等) | 用户拍板实施(2026-09-13)。已按"全量迁移+三订阅+JSON schema+死信+lag"落地;后续若判据仍不成立/运维发现双订阅成本不值,须评估回退(仅 `git revert`,已无配置开关)。points 单事务→三订阅的原子性损失由 `EventProcessed(scope,event_id)` + engine 命名空间幂等兜底,须持续关注重复/漏副作用 |
| 11 | M3.3 | **user_dim 对账每拍 O(用户数) 轻量扫描** | 原单库候选 join 天然分窗、命令恒 3;拆库后无法跨库 join,改为 auth 全量 `(id,updated_at)` + 业务全量 `(user_id,sync_ts)` 求差集(命令恒 4,空 2),内存为 O(N) 轻元组 | 用户量大(十万级+)时周期对账每拍全扫成本上升、内存线性 | 当前接受(报表表、低频日跑);若用户规模上去或对账耗时可见,改为**水位线/游标**(持久化 last_id 或 last_updated_at)分窗增量,消除全扫。届时回本表登记判据 |
| 12 | M5(7.2) | **L1 以自实现有界 TTL 字典替代蓝图点名的 aiocache;L2 分布式互斥锁不做;批量读不覆盖 L1** | 蓝图 §5.6 L1=aiocache 首级;§5.4 可再叠跨进程 L2 锁 double-check | aiocache 当前稳定版(0.12.x) `SimpleMemoryCache` **无 maxsize** 且带 TTL 的 set 注册 loop 绑定 `call_later`,不满足「有界 + 测试可复位」;跨进程 L2 锁失败模式(持锁崩溃/等锁阻塞/续约)多、收益低 | 用户拍板推进 L1+singleflight 时按实际库能力落地(2026-09-13):自实现语义等价(有界 TTL+LRU),L2 锁与批量读 L1 留待后续按需回审 |
| 13 | M5(7.2) | **msgspec 序列化不按蓝图 `to_camel`,保持既有 snake_case** | 蓝图 §6.5.2 写「msgspec(`to_camel`)」 | 现有前端契约(`LKM-official-website/src/lib/api/modules/timeline.ts` 及组件)直接消费 `item_type`/`content_preview`/`next_cursor`,改 camel 即破坏性变更 | 保留 snake_case(msgspec Struct 镜像字段同名同序,2026-09-13);若未来新端点要 camel,再按新契约单独起版本,不动既有 timeline |
| 14 | M5(7.2.1) | **属性测试 oracle 强度与慢 fixture 适配** | hypothesis 属 M5 后置增强,须防假绿 | 不变量过弱则属性测试形同虚设;schema-per-test 慢 fixture + `filterwarnings=["error"]` 易冲突致红 | 注册 `lkm` profile(deadline=None/抑制相关 health check),每对象 ≥2 条独立不变量(退避单调+有界、relay 不重不丢、对账命令恒+收敛),DB 成本高的按 `@settings(max_examples=20~25)` 收紧;`--hypothesis-seed` 可复现(2026-09-13) |
| 15 | M5(7.2.2) | **OTel 显式传 provider 不设全局;instrument 顺序** | 蓝图 OTel→SigNoz 后置;本批只 SDK+Collector,不自托管 SigNoz | `trace.set_tracer_provider` 全进程只能设一次,测试反复 setup 会被忽略;FastAPI instrument 与自研日志中间件顺序可能致 span 缺失 | 显式传 `tracer_provider`(可反复 setup/shutdown 自足回退);日志 trace_id 在活动 span 内断言;真 collector 冒烟(`--profile otel`)与中间件顺序需人工/集成验证(2026-09-13) |
| 16 | M5(7.2.3) | **SecretStr 化 + 生产校验只做条件字段;Infisical 注入以既有 env 优先** | 蓝图 `.env`→Infisical 无明文;M0–M4 不以其为前置 | ①各 worker 进程 env 集不同(worker-scheduler 不接 DB/Redis),按进程强制 db/redis 校验会误杀;②Infisical 注入若覆盖 env 会破坏 compose 显式下发/本地 override;③profile 服务用 `:?` 必填会让未启 profile 的 `compose config` 直接报错 | ①validator 只补条件字段(s3 键/auth seam token),不强制 db/redis;②`os.environ` 已有值优先,Infisical 仅补缺;③profile 服务变量改 `:-` 默认值并文档化(2026-09-13)。引导密钥(client secret)仍需 env 下发,故 Infisical 非"零明文"终点,后续可评估 file/secret 挂载 |
| 17 | M5(7.2.4) | **APISIX standalone:ssl 只收内联 PEM,须 sidecar 渲染;静态 DNS 解析** | 蓝图 nginx→APISIX | ①`ssls[].cert/key` 不能引用证书文件,证书续期须重渲染;②单文件挂载用 mv 换 inode 会让容器看不到更新;③upstream 用 DNS discovery 替代 nginx runtime resolver,实解析行为未真机验证 | ①`render.sh` 每 6h 重渲染证书段并就地写同 inode,APISIX yaml provider 自动 reload;②缺证书时生成自签占位保冷启动;③DNS discovery+6h reload 兜底,**须真机核实 upstream 解析与后端重启后可用**(2026-09-13) |
| 18 | M5(7.2.4) | **微型 acme-webroot 属「完全替换 nginx」的窄例外;登录限流用 local policy** | 用户拍板 APISIX 完全替换;蓝图限流/熔断 | ①APISIX 不服务静态文件,http-01 challenge 需一个静态 responder;②网关限流 policy=local 各实例独立计数,多副本下阈值非全局 | ①保留极小 `nginx:alpine` 仅服务 `/.well-known/acme-challenge/`(不承担网关路由),文档登记为窄例外;②登录限流 policy=local 避免新引入 Redis 依赖,应用层 Redis 防爆破仍在;若多副本需全局阈值再回审(2026-09-13) |
| 26 | M6.1 | **APISIX `cors` 在 `allow_credential=true` 时禁 `*`,违反会让整条路由不加载;而我的静态断言把该错误契约写成了「正确」** | §8 #19 ① 已立过规矩:「静态测试须能挡住启动即败的配置错」,且 #19 就是「静态测试与实现同错→假绿」的实例 | M6.1 把网关 `cors: {}` 改为显式白名单时写了 `allow_headers: "*"` + `allow_credential: true`;`test_apisix_config.py` 同时断言 `assert "*" in cors["allow_headers"]` —— **与实现同错**,28 项静态测试全绿。真机暴露:APISIX 启动日志 `failed to check the configuration of plugin cors err: you can not set '*' for other option when 'allow_credential' is true`,8 条路由**整条被拒载**,流量静默退化为经 astro 转发(API 仍 200 故粗看正常),表现为冒烟 2 项失败:登录限流 429 不触发、WS upgrade 404 | 2026-09-16 修复:①`allow_headers` 改显式列表(`Authorization,Content-Type,Accept,Origin,X-Request-ID,X-Requested-With,Cache-Control,Pragma`);②静态断言改为**按 APISIX schema 规则本身**断言(4 个字段任一为 `*` 即失败),不再照抄实现取值;③冒烟复跑 11/11 绿(含 `login gateway rate-limit 429`、`ws upgrade reaches backend 403`)。**教训**:配置类静态测试要断言「规则」而非「当前取值」,否则实现写错时断言跟着错,与 §8 #19 是同一失败模式(第二次发生) |
| 27 | M6.1(收尾) | **「必须人工双写」的四处收敛:能删的删(应用层 CORS)、不能删的靠分工与单一来源** | 用户问「只保留 APISIX、移除 py 后端相关代码是否可行」 | 逐项核对后发现「双写清单」里混了三种性质:**真冗余**(生产环境应用层 CORS 是死代码——backend/auth/astro 在 compose 里**无对外端口**,流量必经网关)、**不可删的互补**(预签名直传走 `/lkm/*`,而 minio 路由**没有** `client-control` → 应用侧 `max_upload_bytes` 是唯一约束点;`rp_id/origin/redirect_uri` 是应用自身身份)、**本来是不同东西**(网关 `limit-count policy:local` 按 IP 无账号归因 ↔ 应用有用户名级 Redis 滑动窗口)。另发现**应用层 IP 限流早已失效**:各处用 `request.client.host`,在 APISIX 后恒为**网关容器 IP** → `login_ip_max_per_min=20` 实际是「全站 20 次/分」,正常用户会被误锁(应用从不读 `X-Real-IP`,grep 零命中) | 2026-09-16 实施:①**修 IP 限流 bug**——新增 `app/core/client_ip.py`,统一取 `X-Real-IP`(APISIX global rule 以 `$remote_addr` **覆写**该头,不可伪造;直连回落 peer),替换 3 处调用点并删掉 `admin/deps.get_real_client_ip` 重复实现,加 `tests/test_client_ip.py`(6 项);②**CORS 去重**——生产不挂应用层 `CORSMiddleware`(唯一权威=APISIX),`assert_web_security_configured` 不再要求 `LKM_CORS_ORIGINS`,compose 去掉该变量;补静态断言「**凡代理到 backend/auth 的路由都必须带 cors**」(该断言当场抓出 `avatars` 漏配,已补);③**域名单一来源**——模板改 `__COMMUNITY_HOSTS__`/`__OFFICIAL_HOSTS__`/`__ALL_HOSTS__`/`__COMMUNITY_ORIGINS__`/`__COMMUNITY_DOMAIN__` 占位(原先 hosts 硬编码 12 处、CORS 来源 8 处、MinIO Host 1 处),`render.sh` 从 `APISIX_COMMUNITY_DOMAINS`/`APISIX_OFFICIAL_DOMAINS` 展开,并加「残留占位则拒绝覆盖旧配置」守卫;④**上传上限单一来源**——`__MAX_BODY_SIZE__` 由 `LKM_MAX_UPLOAD_BYTES` 展开,compose 让 apisix-render 与 backend 读同一个 env;⑤**限流分工写进文档**(apisix.yaml 该路由处 + `config.py` 该组配置处 + DEPLOYMENT.md),明确「网关=IP 粗粒度削峰(60/60s)、应用=账号级精确锁定(20/200/60s)」,**数值刻意不同、不要同步**。验证:静态测试 24 项(含新增 4 项单一来源断言)、真机改 `APISIX_COMMUNITY_DOMAINS=example.com` 一次即 hosts/CORS/MinIO Host/上限全量跟随、冒烟 11/11 绿 |
| 19 | M5(7.2.4) | **APISIX 运行时验收暴露 7 处配置缺陷 + compose 3 处;并超 nginx 平价开启 `/api/*` WS upgrade** | 静态测试须能挡住启动即败的配置错;平价以旧 nginx 行为为准 | ①静态测试竟把错误契约写进断言(`dns_resolver` 字符串、`role_traditional`),与实现同错→假绿;②`/api/v1/ws/*` 旧 nginx 亦未开 upgrade,经网关 WS 功能不可达(既有隐患而非迁移回归) | ①按真机 3.9 schema 修正 config/apiserver 后回填静态断言(14 项),并加 `enable_websocket` 断言防回归;②在 `api-prefix` 开启 WS upgrade 修复实时端点(超出 nginx 平价、属修 bug,2026-09-14);③转发头用 global rule `proxy-rewrite` 对齐旧 nginx,但**未做 header echo 上游实证**;④`smoke.sh` 定型为带 SNI/绕代理的 13 项运行时断言 |
| 20 | M5(7.2.5) | **Prefect 自托管形态拍板;蓝图该空白(部署形态/验收/回退未定义)须登记** | 蓝图 §调度定死「APScheduler 仅 cron 触发入口、复杂管道由 Prefect flow(DAG/重试/回填)」,但**未定义** server/work pool/deployment 形态、无 Prefect 专属可测验收与回退条款;§8 要求此空白先回审 | 实施者易自行拍板形态(进程内 flow vs 自托管 server vs Docker 镜像)、触发方式(总线消费者直跑 flow vs `run_deployment`)、失败语义 | 用户拍板**一步到位自托管 server+process work pool+deployment**(2026-09-14);触发按 §5.5「cron→总线→消费者」,消费者用 `run_deployment(timeout=0)` 快速触发并 **fail-open 回落直调**;独立 `prefect-postgres` 库、UI 不发布公网端口;回退=`LKM_PREFECT_ENABLED=false`(默认)+ `--profile prefect down`。flow 与 Prefect 解耦(注入式纯编排)以便测试不起 server |

| 21 | M5(7.2.6) | **ClickHouse 形态/schema/查询面全属蓝图空白,须登记** | 蓝图 §6.2 只定「日志/行为/`event_failures` 周期灌 ClickHouse 做分析」,未定义表 schema/TTL/分区、采集组件(vector/fluentd)、部署形态、查询面、落点;§8 要求空白先回审 | 实施者易自拍形态(仅日志 vs 仅导出)、schema、是否暴露查询 API、落点归属 | 用户拍板**两路一次做全**(app 周期导出 + vector 容器日志)**+ admin 只读查询 + 自托管 compose profile**(2026-09-14)。边界取舍:audit 导出落 auth owner(`app/modules/auth/audit_export.py`)、cron 触发落 `auth/tasks.py`、纯体层 `analytics_body.py` **不 import prefect**——实现零 import-linter 契约例外(仅既有 deps 白名单加一行);查询表/列白名单+参数化,CH 未启用/不可达返 503;TTL 固定 30/180/365 天、ReplacingMergeTree 合并前 `count()` 可能短暂重复(水位已防重导);回退=`LKM_CLICKHOUSE_ENABLED=false`(默认)+`--profile clickhouse down`。真机验收(2026-09-15)另抓 3 处静态测试漏网缺陷并修复回填断言:DateTime64 直接作 TTL 致 init.sql 中止(改 toDateTime)/vector 镜像 yaml 优先于 toml 致采集 demo 假日志(显式 --config)/缺 `[async]` extra 致 get_async_client ImportError |
| 22 | M6(全轨) | **新增 M6 补全轨:蓝图缺口按价值/代价补全;并登记 7 项「明确不采纳」** | 蓝图 §一–§十 原有设计;M0–M5 里程碑已排固 | 2026-09-15 逐章实现核对发现:核心链路已落地且部分超出蓝图(RabbitMQ 已删/msgspec/APISIX/Infisical/ClickHouse 已实现),但**一批蓝图正文设计项从未落地**(安全响应头与 CORS/探针分级/Pulsar 复合 readiness/outbox SKIP+LOCKED 与失败分类/GraphQL 防护/by-ids 批量/interaction 收藏与浏览/WS 通用推送/notification 站内信),另有「按需/后置」项未启动(搜索三阶段/timeline 物化/计数链路/CI/CD/Testcontainers/Grafana/TimescaleDB/k8s/SigNoz/RS256) | **M6 非蓝图原定里程碑**,用户要求按价值/代价排序登记(2026-09-15):P0 安全加固+探针、P1 outbox/GraphQL/批读、P2 interaction/WS/notification、P3 判据式(搜索 P1/计数链路/timeline 物化/工程效率);不改 M0–M5。**明确不采纳**:UUID 主键改造、Repository 泛型层、全库软删除、TimescaleDB、k8s 清单、SigNoz 自托管、RS256(理由见 §9.5)。各阶段内嵌回审点:HSTS 仅 production/outbox 归档须先归档后删/Pulsar 探活防拖慢 readiness/by-ids 批量上限/WS 通道越权/通知风暴限速/view_logs 是否落 CH 的分叉 |
| 23 | M6.1/M6.2(P0) | **P0 落地的四处口径偏离,须登记** | §9.2 两卡原文:「生产必填」进 Settings;探针照 `health_auth.py` 范式;/readiness 复合四项 | ①「生产必填」若进 `Settings._no_insecure_secrets_outside_dev` 会因 worker 进程 env 集不同而误杀(§8 #16 同款);②TrustedHost 白名单若只列公网域名会把**容器 healthcheck 直连 127.0.0.1** 判 400 → 长期 unhealthy(探针分级反成故障源);③`cors_origins="*"` 与 `allow_credentials` 并存是浏览器直接拒绝的高危组合;④readiness 对 `disabled` 的处理在「未配置」与「故障」间须显式定档 | ①改由 `Settings.assert_web_security_configured()` 在 `create_app`/`create_auth_app` **HTTP 装配期**强制(worker 不调 → 不误杀),compose 侧给 backend/auth 下发带生产默认值的变量;②`LKM_ALLOWED_HOSTS` 生产默认值含 `backend,auth,localhost,127.0.0.1`,并写入 compose/`.env.example`;③中间件对含 `*` 的白名单**自动关闭** `allow_credentials`(不报错,防配置面落到高危组合);④定档:Pulsar/AUTH 的 `disabled` **不降**就绪,Redis 的 `disabled` **降**就绪(与既有 `/health` 口径一致),`tests/test_health_probes.py` 逐档断言(2026-09-16) |
| 24 | M6.2(P0) | **Pulsar 探活「每请求实探 + 成功结果短缓存」而非心跳新鲜度** | §9.2 风险栏给的兜底是「若探活开销可见,改为 lag reporter 心跳新鲜度判定」 | 心跳新鲜度需引入「lag 上报最后一次成功时刻」的跨模块状态,且 lag 上报周期(默认 30s)远大于 readiness 探针间隔 → 判定粒度粗、恢复慢;而 Admin REST 实探是**真连通性**,更贴合「该不该摘流」的语义 | 先按「短超时(fail-fast)+ 仅成功结果缓存(默认 5s,失败不缓存)」落地(2026-09-16);仅当真机实测 readiness 延迟可见时再改心跳判定,届时回本表登记判据 |

> 本表在使用中也用来当「执行进度 commit 参照」:每次实施推一个大 diff 未在表中登记的新架构决策 → 即违反既定,应回到蓝图审核而不是静默推进。

---

## 9. M6 补全轨:蓝图缺口补齐(价值/代价排序)

> **来源与定位**:2026-09-15 对《后端规划.md》§一–§十 的**逐章实现核对**(方法:逐节对照代码
> Grep/Read,不采信「文件存在即算实现」)。结论:核心链路(认证/消息/缓存/可观测/网关/存储/
> 分析)已落地且部分**超出蓝图**(RabbitMQ 已物理删除、msgspec/APISIX/Infisical/ClickHouse 均已实现);
> 但存在一批蓝图正文**设计而从未落地**的功能,及一批蓝图标注「按需/后置」尚未启动项。
> **M6 不是蓝图原定里程碑**,故不改动 M0–M5 任何阶段卡;本轨独立并行,非任何轨道的前置。
> 口径沿用本文档:P0/P1/P2 为排期项;**P3 为判据式触发(未达不实施)**;§9.5 为**明确不采纳**清单。
>
> **核对口径的已知局限**:单条字段命名/表名差异(如 `BizError` vs 蓝图 `AppError`、日志 stderr vs
> stdout)按「实现取向」处理,不列为缺口;仅「功能有无」进本轨。

### 9.1 优先级总表(先看这张)

| 优先级 | 阶段 | 缺什么 | 价值 | 代价 | 排序理由 |
|---|---|---|---|---|---|
| P0 | M6.1 安全响应头/CORS/HSTS 加固 | 应用层无 TrustedHost/CORS 白名单/SecurityHeaders/HSTS;网关 `cors:{}` 兜底 | 高(公网暴露面) | 低(纯中间件+网关配置,无新依赖) | 风险明确、改动局部、可立即验收 |
| P0 | M6.2 探针分级与 readiness 补全 | 单体无 liveness/readiness 分级端点;readiness 未纳 Pulsar(只探 DB+Redis+AUTH) | 高(探针失真致误判健康/误重启) | 低(照 `health_auth.py` 范式扩) | 运维正确性,与 M6.1 同批 |
| P1 | M6.3 outbox 健壮性补全 | 无 `SKIP LOCKED`;`locked_at/locked_by` 建而不用、无陈旧锁重置;无失败分类;published 无归档 | 中高(一致性兜底) | 中(核心链路,需回归既有 lease 测试) | 现有租约已覆盖主场景,属补强非救火 |
| P1 | M6.4 GraphQL 防护与指标 | 无深度/复杂度限制、无查询超时、无 GQL 指标(N+1 亦无 DataLoader) | 中高(公网只读端点可被打穿) | 中(strawberry 扩展+阈值校准) | 防护缺失是真实风险,成本可控 |
| P1 | M6.5 批量跨 AUTH 读 | 无 `by-ids` 批量端点,回退读是逐 id HTTP 循环 | 中(消 N+1) | 低(增端点+改 seam) | 小改动直击 AUTH 打满风险(M3.4 风险栏已预判) |
| P2 | M6.6 interaction 模块 | 无收藏、无浏览记录 `view_logs` | 高(业务功能缺失) | 中高(新模块+表+缓存+保留策略) | 业务侧可见功能,越早越好 |
| P2 | M6.7 WS 通用推送 + 幂等字段 | 通道仅 `ws:upload:`;推送无 `version/event_id` 去重字段 | 高(M6.8 前置) | 中(复用既有 manager/broker) | 是 notification 的地基,必须先做 |
| P2 | M6.8 notification 模块 | 站内信/偏好/推送 token 三表与模块全无 | 高(业务功能缺失) | 高(依赖 M6.7,含扇出限速) | 依赖 M6.7;体量最大放 P2 末 |
| P3 | M6.9 搜索 P1(PG FTS) | 无任何检索能力(tsvector/GIN 零命中) | 中 | 中 | 判据式:检索需求成立再做,只做 P1 |
| P3 | M6.10 互动计数 Redis 链路+对账 | 计数为 `like_count += 1` 直改 DB,无对账收敛 | 中(按流量) | 高(新链路+对账+迁移) | 判据式:低流量下 DB 直改可用 |
| P3 | M6.11 timeline 物化读模型 | read-time 多源合流,无物化表/页缓存 | 中(按数据量) | 高 | 判据式:读延迟/成本超预算再启 |
| P3 | M6.12 工程效率(CI/CD+Testcontainers+Grafana) | 无 CI 流水线、无 Testcontainers、无 Grafana 面板 | 中(团队效率) | 中(可拆滚进) | 不阻塞业务,随团队规模补 |

> **代价说明**:「低」≈1–2 天(纯应用层,静态测试可覆盖);「中」≈需改核心链路或加新端点,须回归+
> 可能真机验收;「高」≈新模块/新基础设施/数据迁移,须真机端到端。

> **进度(2026-09-16)**:P0 两项 **M6.1 / M6.2 已落地代码、静态验收 + 真机验收,状态置 [已验收]**
> (重建 backend/auth 后逐头断言、Host 白名单拒答、CORS 白名单实探、AUTH/Redis 故障注入与
> `RestartCount=0` 均已复核;APISIX 网关侧运行时冒烟因网关容器**先于本批即 `Exited`** 未跑,
> 见 §9.2 M6.1 卡末)。P1/P2/P3 仍未启动。

### 9.2 P0 — 安全与可运维性(无前置,可立即开工)

#### M6.1 — 安全响应头 / CORS 白名单 / HSTS 加固
| 状态 | [已验收] | 依赖 | 无(M0 基座) | 泳道 | M6 |

> **实际执行(2026-09-16)**:新增 `app/core/middleware.py`(`SecurityHeadersMiddleware` 纯 ASGI +
> `install_security_middleware(app)` 共用装配函数),**单体 `app.main` 与 auth 进程 `app.main_auth`
> 同源装配**(防两处语义漂移)。`Settings` 增 `allowed_hosts`/`cors_origins`(逗号分隔)与
> `allowed_hosts_list`/`cors_origins_list`/`assert_web_security_configured()`。APISIX 侧 8 处
> `cors: {}` 全部替换为显式白名单(allow_origins/methods/headers/expose_headers/max_age/
> allow_credential)。compose 给 backend/auth 下发 `LKM_ALLOWED_HOSTS`/`LKM_CORS_ORIGINS`(带生产
> 默认值),`.env.example` 记录。

**动作(含落点)**:`app/main.py` 装配三个中间件——`TrustedHostMiddleware`(allowed_hosts 走 Settings)、
`CORSMiddleware`(**显式 origin 白名单**,禁 `*` 与 `allow_credentials` 并存)、自研
`SecurityHeadersMiddleware`(`X-Content-Type-Options: nosniff`、`X-Frame-Options: DENY`、
`Referrer-Policy`、`Permissions-Policy`;**HSTS 仅 production**)。新增 Settings
`LKM_ALLOWED_HOSTS`/`LKM_CORS_ORIGINS`(逗号分隔,默认 dev 宽松、生产必填)。APISIX 侧
`deploy/apisix/apisix.yaml` 的 `cors: {}` 改为显式 origins/headers/methods(与应用层一致)。

**验收(可测)**:`curl -I` 断言四响应头存在、`Strict-Transport-Security` **仅** `LKM_ENV=production`
出现;非白名单 Origin 的预检 `OPTIONS` 被拒;`tests/test_security_middleware.py` 逐头断言;
`tests/deploy/test_apisix_config.py` 增 cors 白名单断言(防回归到 `{}`)。

**风险与回审**:HSTS 在纯 HTTP 环境误开会把域名锁死到 https → 严格 `is_production` 门控,
并登记 §8 #22。CORS 白名单若漏配前端域名会致跨域全挂 → 默认值须含官网域名并写进
`.env.example`。

**落地偏离(登记 §8 #22)**:①「生产必填」**不进** `Settings` 的逐进程校验器(worker 进程 env 集
不同,强校验会误杀,与 §8 #16 同款取舍),改由 `assert_web_security_configured()` 在
`create_app`/`create_auth_app` 装配期强制——只对真正承载 HTTP 的进程生效;②`LKM_ALLOWED_HOSTS`
生产默认值**必须含内网服务名/回环**(`backend,auth,localhost,127.0.0.1`),否则容器 healthcheck
直连 `127.0.0.1:8000` 被 TrustedHost 判 400 → 长期 unhealthy;③`cors_origins` 含 `*` 时中间件
**自动关闭** `allow_credentials`(而非报错),保证通配配置也不会落到「`*`+凭证」高危组合。

> **真机验收(2026-09-16,`docker compose up -d --build backend auth` 重建后)**:①直连 backend
> `/api/v1/liveness` 响应含 `X-Content-Type-Options: nosniff` / `X-Frame-Options: DENY` /
> `Referrer-Policy` / `Permissions-Policy`;生产(`LKM_ENV=production`)下 `Strict-Transport-Security:
> max-age=31536000; includeSubDomains` 存在,auth 进程同款头亦在;②Host 白名单外(伪造 `Host: evil.example`)
> → **400 且拒答响应仍带安全头**(证最外层装配生效);白名单内 `127.0.0.1` 正常 200(证 healthcheck
> 不被误杀);③CORS 实探:白名单 `https://lkm-ahz.ltd`/`https://www.lkm-ahz.ltd` → `ACAO` 回显 +
> `Allow-Credentials: true`;`https://evil.example` → 无 `ACAO`;无 Origin → 无 CORS 头。
> **网关侧运行时验收(2026-09-16 补)**:APISIX 起回后跑 `sh deploy/apisix/smoke.sh` **11/11 绿**
> (含 `login gateway rate-limit 429`、`ws upgrade reaches backend 403`)。**首轮曾 2 项失败**——
> 网关 `cors` 的 `allow_headers: "*"` 与 `allow_credential: true` 冲突致 8 条路由整条被拒载,
> 且当时静态断言把该错误契约写成「正确」故全绿假象;已修配置 + 把断言改为按 schema 规则断言,
> 详见 §8 #26。

#### M6.2 — 探针分级(liveness/readiness)与 Pulsar 复合
| 状态 | [已验收] | 依赖 | M0 | 泳道 | M6 |

> **实际执行(2026-09-16)**:`app/modules/health/router.py` 增 `/liveness`(零外部依赖)与
> `/readiness`(复合 DB+Redis+Pulsar+AUTH,**未就绪返 503**);`app/core/pulsar_lag.py` 增
> `probe_health()`(Admin REST `/admin/v2/brokers/health`,短超时 fail-fast + **仅成功结果**短缓存)
> 与可注入 `_probe_client_factory`。compose `backend` healthcheck 由 `/api/v1/health` 改打
> `/api/v1/liveness`。新增 `tests/test_health_probes.py`(16 项)。

**动作(含落点)**:单体新增 `/liveness`(零外部依赖)与 `/readiness`(复合 DB+Redis+**Pulsar**+AUTH,
AND 语义),照 `app/health_auth.py` 范式落 `app/modules/health/router.py` 或新 `app/health.py`;
Pulsar 探活复用 `app/core/pulsar_lag.py` 的 Admin REST 通道(短超时、fail-fast),不新起连接。
compose `healthcheck` 改打 `/liveness`(避免依赖抖动致容器被判死)。

**验收(可测)**:故障注入(停 DB/Redis/Pulsar/AUTH 任一)→ 进程**不重启**且 `/liveness` 200、
`/readiness` **503**;全部恢复 → `/readiness` 200;Pulsar 未配置(空串)时 readiness 语义明确
(不计入硬依赖)。`tests/test_health_probes.py`。

**风险与回审**:Pulsar 探活若走同步 Admin REST 可能拖慢 readiness → 短超时 + 缓存上次结果;
若探活开销可见,改为「lag reporter 心跳新鲜度」判定而非每请求实探(登记 §8 #22)。

**落地口径(登记 §8 #22)**:①`disabled`(未配置)对 Pulsar/AUTH **不降就绪**(部署取向而非故障),
但 **Redis 是硬依赖**——`disabled` 亦返 503(与既有 `/health` 口径一致:L2 缺失即无一致性锚点);
②探针端点**不经** `ApiResp` 信封(与 `health_auth` 报文一致,便于 compose/编排直接读 `status`);
③缓存只存成功结果,失败不入缓存 → 恢复立即可见且不会把 stale 健康当就绪;④探活仍是**每请求实探**
(5s 内复用成功结果),未改为「心跳新鲜度」判定——真机若见开销再加,届时回本表登记。

> **真机验收(2026-09-16,重建后容器内直探)**:①基线 `/readiness` **200**,四项
> `db/redis/pulsar/auth` 全 `up`(含真实 Pulsar Admin REST `/admin/v2/brokers/health` 探通);
> ②**故障注入**:`docker compose stop auth` → `/liveness` 仍 **200**、`/readiness` **503**
> (`auth` 项报不可达);`stop redis` → `/readiness` **503**(`redis: error`)、`/liveness` 仍 200;
> 两者恢复后 `/readiness` 回 **200**;③**进程确实未重启**:全程 `docker inspect` 的
> `RestartCount=0` 且 `StartedAt` 保持重建时刻不变;④auth 进程 `/liveness`+`/readiness` 200,
> compose `auth` healthcheck 未受影响(仍打其自己的 `/liveness`)。

### 9.3 P1 — 一致性与接口防护

#### M6.3 — outbox 健壮性补全
| 状态 | [待启动] | 依赖 | M1(已验收) | 泳道 | M6 |

**动作(含落点)**:`app/core/outbox_relay.py` ① 领取改 `with_for_update(skip_locked=True)`
(与既有 leader 租约叠加,作为 leader 内多线程兜底);② **真正使用** `locked_at/locked_by`
(领取即写、投递后清),并实现**陈旧锁超时重置**(超 `LKM_OUTBOX_LOCK_TTL_S` 视为可抢,防持锁进程崩溃
后该行永久卡死);③ **失败分类**:`except` 分支区分「可重试(网络/超时/总线不可达)」与「永久失败
(schema 校验/序列化/未知 routing_key)」,后者一次即折叠进 `event_failures` 不空耗重试;
④ **published 归档**:定期删除/迁移已发布且超期的行(替代蓝图 Timescale hypertable 的轻量方案,
避免表无限增长)。

**验收(可测)**:两 relay 并发抢同一行 → `SKIP LOCKED` 下不重复投递(按 event_id 去重断言);
手工置 `locked_at` 为过期 → 该行被接管;注入永久失败 → `attempt_count` 不累加即入 `event_failures`;
归档跑后 published 行数下降而 pending/failed **不受影响**。扩 `tests/test_outbox_lease.py` +
`tests/test_outbox_props.py`(属性测试加「不重复投递」不变量)。

**风险与回审**:`skip_locked` 会改变既有阻塞式语义 → 必须回归 M1.1/M1.2 全部 lease 测试;
归档删除属**不可逆**操作,须先归档到 CH/冷表再删(登记 §8 #22)。

#### M6.4 — GraphQL 防护与可观测
| 状态 | [待启动] | 依赖 | M2.3(只读聚合已验收) | 泳道 | M6 |

**动作(含落点)**:`app/api/graphql.py` 增 strawberry 扩展——`QueryDepthLimiter`(阈值走
`LKM_GRAPHQL_MAX_DEPTH`)、复杂度/成本上限、查询级超时;`app/core/metrics.py` 增
`graphql_query_duration_seconds`/`graphql_query_rejected_total`;N+1 热点(feed/content 列表字段)
按实测决定是否上 DataLoader(不预设)。

**验收(可测)**:超深/超复杂查询返回**受控错误**(非 500、非栈);慢查询被超时切断且连接释放;
`/metrics` 出现两个新指标且被拒时 `rejected_total` 递增;`tests/test_graphql_guard.py`。

**风险与回审**:阈值过严会误拒现有前端查询 → 用现有查询集实测校准后写死默认值并登记。

#### M6.5 — 批量跨 AUTH 读(by-ids)
| 状态 | [待启动] | 依赖 | M3.2(user:snap 已验收) | 泳道 | M6 |

**动作(含落点)**:AUTH 侧 `app/modules/auth/router_read.py` 增
`GET /auth/internal/users/by-ids?ids=...`(≤200/批、去重、内部 token only,返回只读 snapshot 列表);
主服务 `app/modules/auth/user_http.py` 增批量方法,`app/modules/auth/snapshot.py` 批量路径改
「先批量缓存读 → 未命中批内 singleflight → 一次 HTTP」,替代现有逐 id 循环。

**验收(可测)**:批量 N 个 id 的 AUTH HTTP 调用数 = `⌈N/200⌉`(断言调用计数);全部命中 L1/L2 时
**零 HTTP**;超限 ids 被拒或分批;`tests/test_auth_user_snapshot_http.py` 扩。

**风险与回审**:批量端点放大攻击面(大 ids 数组)→ 强制上限 + 内部 token + 去重;若 AUTH 侧
成新瓶颈,回到「批内 singleflight + 退避」而非加大批量(登记 §8 #22)。

### 9.4 P2 — 业务功能补全

#### M6.6 — interaction 模块(收藏 + 浏览记录)
| 状态 | [待启动] | 依赖 | M2.2(内容域已收束) | 泳道 | M6 |

**动作(含落点)**:新 `app/modules/interaction/`(`models.py`/`router.py`/`service.py`/`schemas.py`),
表 `favorites(user_id, content_id)` 复合主键(天然幂等)+ `view_logs(user_id, content_id, viewed_at)`;
登记 `app/modules/registry.py`(路由聚合)+ `app/db/model_registry.py`(模型预注册)+ 新权限点;
REST:收藏/取消/我的收藏列表、浏览上报(upsert 幂等)。`view_logs` 高频写须给保留策略。

**验收(可测)**:重复收藏不报错且不重复计数(复合主键断言);浏览上报同内容重复调用幂等;
未登录/无权限被拒;`tests/test_interaction.py` + `tests/test_interaction_rbac.py`。

**风险与回审**:`view_logs` 写放大 → 评估「明细直接进 ClickHouse、PG 只留最近窗口」;
若走 CH 则本表仅留聚合,须在 §8 登记该分叉(避免两处真相)。

#### M6.7 — WS 通用推送通道 + 幂等字段
| 状态 | [待启动] | 依赖 | M3(WS 已验收) | 泳道 | M6 |

**动作(含落点)**:`app/ws/broker.py` 通道由 `ws:upload:{id}` 泛化为 `ws:{user_id}:{channel}`
(**保留 upload 通道兼容**);推送 payload 统一加 `version` + `event_id`(前端据此去重);
`app/ws/manager.py` 维持按 user_id 多连接+多通道订阅;订阅侧逐通道校验权限(只订阅自己的通道)。

**验收(可测)**:两通道并发推送不串扰(各自只收到自己的);同 `event_id` 重推时 payload 一致可供
前端去重;既有上传进度用例**不回归**;`tests/test_ws_channels.py` + `tests/test_ws_upload.py` 扩。

**风险与回审**:通道泛化后越权订阅是安全面 → 通道名由服务端派生(user_id 前缀强制),
不接受客户端自选;登记 §8 #22。

#### M6.8 — notification 模块(站内信 / 偏好 / 推送)
| 状态 | [待启动] | 依赖 | **M6.7**(硬前置) + M2.3 | 泳道 | M6 |

**动作(含落点)**:新 `app/modules/notification/`;表 `notifications(user_id, type, payload, read_at)`、
`notification_preferences(user_id, type, enabled)`、`notification_tokens(user_id, token, platform)`;
产生通知 = 写库 + 经 M6.7 通道实时推送;偏好门控(关则只落库不推或按类型不落);消费既有业务事件
(feed/points/content 的 outbox 事件)生成通知。

**验收(可测)**:产生通知 → 入库 + WS 收到(集成测试);关偏好后不再推送;已读状态可写可查;
`tests/test_notification.py` + 事件消费幂等用例。

**风险与回审**:**通知风暴**(一次动作扇出大量通知)→ 需限速/同类聚合(same target 合并);
推送与库不一致 → 以库为准、推送 best-effort;登记 §8 #22。

### 9.5 P3 — 判据式触发项与「明确不采纳」清单

#### P3 触发闸门(未达判据不实施)

| 项 | 触发判据 | 动作要点 |
|---|---|---|
| M6.9 搜索 P1(PG FTS) | 站内检索需求成立(产品确认) | `content_items` 加 `tsvector` + GIN,`/api/v1/search` 只读聚合;**只做 P1**;P2/P3(Meilisearch/OpenSearch)判据见蓝图 §6.5.3 |
| M6.10 互动计数 Redis 链路 | 计数写 QPS 超 DB 直改舒适区,或热点行争用可见 | Redis `INCR` 单向增量 + 批量落库 + `cron.reconcile.counts` 对账收敛(`last_reconciled_at`);对账口径须可证伪(抽验 diff=0) |
| M6.11 timeline 物化读模型 | feed 读延迟/成本超预算 | 物化表 + `created_at+id` 游标 + `feed:{user}:{cursor}` L2 页缓存;实时合流降为兜底 |
| M6.12 工程效率(CI/CD + Testcontainers + Grafana) | 团队协作回归成本上升 | GitHub Actions(ruff→pytest→迁移可回滚→镜像)、Testcontainers 集成(PG+缓存+Pulsar)、Grafana 基础面板(QPS/P50/P95/错误率/outbox 积压) |

#### 明确不采纳(经本次核对判定,不进 M6;理由登记 §8 #22)

| 蓝图项 | 不采纳理由 |
|---|---|
| **UUID 主键全库改造**(蓝图 §3.1) | 现全库 Integer 自增已铺开(含 auth 库与业务库双链),改造成本极高、迁移风险大、无业务收益;`outbox.event_id` 用 UUID 已满足事件幂等需求 |
| **Repository 泛型层**(§3.3/§7.1) | service 直写 SQLAlchemy 已是全库惯例且测试充分,抽层属纯形式收益,徒增间接层 |
| **全库软删除 mixin**(§3.1) | 仅少数表(评论/帖子)有恢复诉求,按需加列即可,不值得全库铺开 |
| **TimescaleDB 连续聚合**(§6.2) | 分析场景已由 ClickHouse(日志/失败事件/审计)+ Prometheus(指标)覆盖,无独立时序聚合需求 |
| **k8s 清单 + startup/readiness/liveness 三分探针**(§6.4) | 当前部署形态是单机 compose,无 k8s 运行时;M6.2 已把探针**语义**补齐,上 k8s 时再产出清单 |
| **SigNoz 自托管**(§6.2) | OTel SDK + Collector 已就绪(`--profile otel` 真机验证过),接外部后端即可,自托管运维成本不划算 |
| **RS256/JWKS + 网关验签**(§2/§4.2) | 单集群内部签发/验签,HS256 共享密钥在既定信任边界内足够;跨集群或第三方验签需求出现时再启(届时需密钥轮换+灰度方案) |

> M6 全部阶段完成后,把状态回填到 §9.1 总表与本表,并在 §8 更新 #22。

---

## 10. 人工核对清单(输出后让你照着 LKM-service 对一遍落点)

交付本文件后,建议对下列措辞与现有代码核对修正(本 agent 未访问 LKM-service,凡与实际模块名/文件/队列不符时按 Reference「实际代码边界」修正后再验收):

**核对完成(2026-09-13,对照 LKM-service commit `9fd2dae`)**——主要修正:

- M3.3 **无 `etl/` 目录**:ETL 实际落 `app/modules/auth/user_dim_sync.py`(数据源 owner 侧),宽表 `app/db/user_dim.py`,离线只读 port `app/modules/admin/dim_report.py`。
- 队列已由 RabbitMQ **全量迁 Pulsar**(§7.1 / §8 #10),逻辑 routing_key→topic 映射见 `app/core/messaging.py`。
- AUTH 发出事件名 `user.updated / banned / session_revoke` 与 `app/modules/auth/events.py` 一致。
- 内容/信息流域实际目录为 `app/modules/content/`(含 boards/columns/qa 子包)与 `app/modules/feed/`;原 `forum` 已删除。
- `app/modules/auth` 已是**独立库**(非单体本体),入口 `app/main_auth.py` + `app/db/auth_base.py`。
- **M3.1 补漏(2026-09-13)**:拆库后 auth 库 schema 初始化链路曾缺失——单体 `init_db` 只建业务库、`main_auth` lifespan 原先明确不建表,而 `alembic_auth/versions/` 为空,compose 生产路径 `LKM_USE_ALEMBIC=false` 下 `lkm_auth` 无表。已补:auth 进程启动调 `init_auth_db`(非 alembic→`AuthBase.create_all`,alembic→第二迁移链,独立锁 key),`alembic_auth/versions/a0b1c2d3e4f5_auth_baseline.py` 落基线(18 表,可升可回滚)。
- **内部读写缝接线补漏(2026-09-13)**:①`router_read.py`/`router_authz.py`(`/auth/internal/*`)原用业务 `get_session` 查 auth 表(注释自认待 S5 切会话却未改)→ 改 `get_auth_session`;②`health_auth.probe_db` 原探业务引擎 → 改探 auth 引擎;③compose `backend` 未配 `LKM_AUTH_HTTP_URL/TOKEN`,seam 关闭时业务库无 `users` 表、snapshot/grant 回落直查会 `UndefinedTable` → 已给 backend 与 auth 两侧注入同值 token(baseline `.env` 已生成),`.env.example`/README 标注生产必填。测试经 seam 替身(`auth_seam_realm`)故原先无感,已同步内部端点用例改用 `get_auth_session`。
- **M3.3 user_dim ETL 跨库修复(2026-09-13)**:`user_dim_sync` 原在**单会话**里跨库 JOIN `User`(auth)与 `UserDim`(业务),融合态测试掩盖、真拆库必 `UndefinedTable`。已改**跨 realm 双会话**:入口接收 `(source_db=auth 读源, target_db=业务写 dim)`;`reconcile` 的跨库 join 改为「auth `(id,updated_at)` 全量 + 业务 `(user_id,sync_ts)` 全量」两次轻量扫描求差集(命令恒 4/拍,空 2,原为 3/1);`_session_factory` seam 改产会话对;jobs worker(`worker` 服务)补 `LKM_AUTH_DB_*`。新增真双库验收 `tests/test_user_dim_split_realm.py`。局限性(已知):对账每拍扫 O(用户数) 轻元组,更大规模再引水位线——登记 §8 #11。
- M1.1 `outbox_events` 表字段(`locked_at/locked_by` 等)与 `app/db/outbox.py` 一致(见 M1.1 卡)。

- [ ] §0 演进基石 M0 现在做到了**哪些**(指针总 M0 状态是否已绿)
- [ ] M1.1 `outbox_events` 表命名与 §5.1 现状模型一致;字段 `locked_at/locked_by` 存在性
- [ ] M3.0/M3.1 `app/modules/auth` 实际存在的内聚文件/auth 服务抽取路径(现状 auth 在单体 app/modules/auth 并在本体)
- [ ] M3.3 `etl/` 现无此目录(规划新增,不含本文档假设已存在)
- [ ] 队列名(RabbitMQ `lkm.send/notify/points/jobs` + dlq)与 §5.2 映射核对
- [ ] AUTH 发出事件名 `user.updated/banned/session_revoke` 与现有 auth 内已发 RabbitMQ 事件清单差异登记
- [ ] missing 文件问题:记忆 Reference 说 `docs/` 下缺 `模块边界与GraphQL适配分析.md`——凡引用该文处确认或改引不打开死链

> 完成后把 §10 核对产生的修订合回对应阶段卡;不改任何已拍板架构实质。
