apiVersion: 1

providers:
  - name: lkm
    orgId: 1
    folder: LKM
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      # dashboard JSON 目录（与 provider 配置分离，避免把本 yml 当面板加载）
      path: /var/lib/grafana/dashboards
