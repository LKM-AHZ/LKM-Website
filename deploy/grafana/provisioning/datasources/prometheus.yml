apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus  # 与 dashboard JSON 的 datasource.uid 对应
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
